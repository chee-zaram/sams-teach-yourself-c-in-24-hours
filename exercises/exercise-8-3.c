/* to display the result of expressions usiing the shift operators */
#include <stdio.h>

int main()
{
    int x, y;
    x = 123;
    y = 4;
    printf("x << y yields: %d\n", x << y);
    printf("x >> y yields: %d\n", x >> y);
    return 0;
}
